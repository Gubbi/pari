[workspace]
members = [".", "xtask"]

[package]
name = "pari"
version = "0.1.0"
edition = "2021"

[lib]
name = "pari"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
schemars = "0.8"

[dev-dependencies]
jsonschema = "0.45"
serde_json = "1"
