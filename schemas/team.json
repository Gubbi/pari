{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Team",
  "description": "A roster binding concrete handles (people or agents) to roles.\n\nWhere [`Role`] describes *what* a position does, `Team` describes *who* fills it. Composition via `include` and `import` lets larger orgs build teams from smaller ones without duplicating membership. Workflow execution resolves a `Role` reference to an actual handle by consulting the active team.",
  "type": "object",
  "required": [
    "entity_ref",
    "name"
  ],
  "properties": {
    "description": {
      "type": [
        "string",
        "null"
      ]
    },
    "entity_ref": {
      "$ref": "#/definitions/TeamEntityRef"
    },
    "import": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "$ref": "#/definitions/TeamEntityRef"
      }
    },
    "include": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": "array",
        "items": [
          {
            "$ref": "#/definitions/TeamEntityRef"
          },
          {
            "$ref": "#/definitions/RoleEntityRef"
          }
        ],
        "maxItems": 2,
        "minItems": 2
      }
    },
    "members": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "$ref": "#/definitions/TeamMember"
      }
    },
    "name": {
      "type": "string"
    }
  },
  "patternProperties": {
    "^x-": true
  },
  "additionalProperties": false,
  "definitions": {
    "RoleEntityRef": {
      "title": "RoleEntityRef",
      "type": "object",
      "required": [
        "id",
        "kind"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$"
        },
        "kind": {
          "type": "string",
          "enum": [
            "Role"
          ]
        }
      }
    },
    "TeamEntityRef": {
      "title": "TeamEntityRef",
      "type": "object",
      "required": [
        "id",
        "kind"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$"
        },
        "kind": {
          "type": "string",
          "enum": [
            "Team"
          ]
        }
      }
    },
    "TeamMember": {
      "type": "object",
      "required": [
        "handle",
        "role"
      ],
      "properties": {
        "handle": {
          "type": "string",
          "pattern": "^@[a-z0-9._-]+$"
        },
        "role": {
          "$ref": "#/definitions/RoleEntityRef"
        }
      },
      "additionalProperties": false
    }
  }
}
